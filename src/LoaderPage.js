import Loader from "./Loader";
import './App.css';

const LoaderPage = () => {
    return(
    <div className="container-loader">
        <Loader />
    </div>
    )
}

export default LoaderPage;